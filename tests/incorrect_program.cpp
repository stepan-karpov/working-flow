#include <algorithm>
#include <iostream>
#include <vector>

void Init() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);
  std::cout.tie(nullptr);
}

void Solve() {
  int n;
  std::cin >> n;
  std::vector<std::string> sequence;

  


}

int main() {
  Init();
  Solve();
  return 0;
}