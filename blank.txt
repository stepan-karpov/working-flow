2
bcab
cab

  // std::cout << sequence << "\n";
  // for (int i = 0; i < sufmas.size(); ++i) {
  //   std::cout << sufmas[i] << " ";
  // }
  // std::cout << "\n";
  // for (int i = 0; i < lcp.size(); ++i) {
  //   std::cout << lcp[i] << " ";
  // }
  // std::cout << "\n";
  // for (int i = 0; i < lcp.size(); ++i) {
  //   std::cout << string_number[sufmas[i]] << " ";
  // }
  // std::cout << "\n";
